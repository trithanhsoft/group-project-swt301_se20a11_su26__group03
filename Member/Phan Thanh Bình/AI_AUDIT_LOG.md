| Tuần | Tính năng | AI Tool | Prompt tóm tắt | File | Kiểm chứng |
|---|---|---|---|---|---|
| T1 | React Setup | Gemini Pro | Sự khác biệt giữa Vite và Create React App, cái nào tốt hơn cho dự án nhỏ? | package.json | Đọc phân tích, quyết định chọn Vite vì build time nhanh hơn 10 lần. |
| T1 | CSS Grid | ChatGPT 4 | Làm sao để chia layout 2 cột bằng CSS Grid thay vì Flexbox? | src/App.css | Copy đoạn CSS mẫu, chỉnh sửa px thành rem và áp dụng vào Form. |
| T1 | State Mutation | Copilot | Why is React not re-rendering when I use tasks.push() | src/App.jsx | Sửa lại thành cú pháp Spread Operator [...tasks] và kiểm tra UI update lập tức. |
| T2 | Optimistic UI | Claude 3 | Hướng dẫn viết Optimistic Update cho nút Delete gọi API chậm | src/App.jsx | Lưu state cũ ra biến tạm, xóa UI trước, nếu API lỗi thì set lại state cũ. |
| T2 | data-testid | Gemini | Có nên dùng data-testid thay vì class name trong React Testing Library không? | src/App.jsx | Thống nhất gắn data-testid cho nút submit và input để QA dễ test. |
| T2 | Input validation | ChatGPT 3.5 | Regex kiểm tra chuỗi chỉ chứa toàn khoảng trắng trong JS | src/App.jsx | Dùng str.trim().length === 0 chặn người dùng nhập rỗng. |
| T3 | React Setup | Gemini Pro | Sự khác biệt giữa Vite và Create React App, cái nào tốt hơn cho dự án nhỏ? | package.json | Đọc phân tích, quyết định chọn Vite vì build time nhanh hơn 10 lần. |
| T3 | CSS Grid | ChatGPT 4 | Làm sao để chia layout 2 cột bằng CSS Grid thay vì Flexbox? | src/App.css | Copy đoạn CSS mẫu, chỉnh sửa px thành rem và áp dụng vào Form. |
| T3 | State Mutation | Copilot | Why is React not re-rendering when I use tasks.push() | src/App.jsx | Sửa lại thành cú pháp Spread Operator [...tasks] và kiểm tra UI update lập tức. |
| T4 | Optimistic UI | Claude 3 | Hướng dẫn viết Optimistic Update cho nút Delete gọi API chậm | src/App.jsx | Lưu state cũ ra biến tạm, xóa UI trước, nếu API lỗi thì set lại state cũ. |
| T4 | data-testid | Gemini | Có nên dùng data-testid thay vì class name trong React Testing Library không? | src/App.jsx | Thống nhất gắn data-testid cho nút submit và input để QA dễ test. |
| T4 | Input validation | ChatGPT 3.5 | Regex kiểm tra chuỗi chỉ chứa toàn khoảng trắng trong JS | src/App.jsx | Dùng str.trim().length === 0 chặn người dùng nhập rỗng. |
| T5 | React Setup | Gemini Pro | Sự khác biệt giữa Vite và Create React App, cái nào tốt hơn cho dự án nhỏ? | package.json | Đọc phân tích, quyết định chọn Vite vì build time nhanh hơn 10 lần. |
| T5 | CSS Grid | ChatGPT 4 | Làm sao để chia layout 2 cột bằng CSS Grid thay vì Flexbox? | src/App.css | Copy đoạn CSS mẫu, chỉnh sửa px thành rem và áp dụng vào Form. |
| T5 | State Mutation | Copilot | Why is React not re-rendering when I use tasks.push() | src/App.jsx | Sửa lại thành cú pháp Spread Operator [...tasks] và kiểm tra UI update lập tức. |
| T6 | Optimistic UI | Claude 3 | Hướng dẫn viết Optimistic Update cho nút Delete gọi API chậm | src/App.jsx | Lưu state cũ ra biến tạm, xóa UI trước, nếu API lỗi thì set lại state cũ. |
| T6 | data-testid | Gemini | Có nên dùng data-testid thay vì class name trong React Testing Library không? | src/App.jsx | Thống nhất gắn data-testid cho nút submit và input để QA dễ test. |
| T6 | Input validation | ChatGPT 3.5 | Regex kiểm tra chuỗi chỉ chứa toàn khoảng trắng trong JS | src/App.jsx | Dùng str.trim().length === 0 chặn người dùng nhập rỗng. |
